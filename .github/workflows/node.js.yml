name: Build Then Deploy

on:
  push:
    branches: [ master, gatsby ]

jobs:
  build-then-deploy:
    runs-on: self-hosted

    strategy:
      matrix:
        node-version: [12.x]

    steps:
      - uses: actions/checkout@v2

      - name: Cache node_modules
        uses: actions/cache@v2
        env:
          cache-name: cache-node-modules
        with:
          # npm cache files are stored in `~/.npm` on Linux/macOS
          path: node_modules
          key: ${{ runner.os }}-${{ matrix.node-version }}-${{ env.cache-name }}-${{ hashFiles('package-lock.json') }}
          restore-keys: |
            ${{ runner.os }}-${{ matrix.node-version }}-${{ env.cache-name }}-

      - name: Use Node.js ${{ matrix.node-version }}
        uses: actions/setup-node@v1
        with:
          node-version: ${{ matrix.node-version }}

      - run: npm install
      - run: npm run build --if-present

      - name: rsync deployments
        uses: burnett01/rsync-deployments@4.0
        with:
          switches: -avprtuzC
          path: public/
          remote_path: /home/simon/vhosts/overcache.com/
          remote_host: overcache.com
          remote_port: 11235
          remote_user: simon
          remote_key: ${{ secrets.DEPLOY_KEY }}
